var RunningAppointmentService = angular.module('RunningAppointmentService', []).service('RunningAppointment', function ($http, $localStorage, $location) {
    this.isRunning = function () {
        var URL = 'https://app.hashtrack.io/api/';
        
    }
});